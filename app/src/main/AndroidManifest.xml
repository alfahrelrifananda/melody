<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:name=".ui.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.melody">
<!--        <activity-->
<!--            android:name=".MainActivity"-->
<!--            android:exported="true"-->
<!--            android:label="@string/app_name">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
<!--        </activity>-->

        <activity
            android:name=".ui.pages.welcome.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"/>

        <activity
            android:name=".ui.pages.nowplaying.NowPlayingActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:launchMode="singleTop" />

        <activity android:name=".ui.album.AlbumDetailActivity" />
        <activity android:name=".ui.artist.ArtistDetailActivity" />
        <activity android:name=".ui.collection.CollectionDetailActivity" />

        <activity
            android:name=".ui.pages.settings.SettingsActivity"
            android:exported="false" />

        <activity android:name=".ui.pages.all.AllSongsActivity"   android:exported="false" />
        <activity android:name=".ui.pages.all.AllAlbumsActivity"  android:exported="false" />
        <activity android:name=".ui.pages.all.AllArtistsActivity" android:exported="false" />
        <service
            android:name=".service.MusicService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <receiver
            android:name=".widget.MelodyWidget"
            android:exported="true">
            <intent-filter>
                <!-- Standard system broadcast when a widget needs updating -->
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />

                <!-- Our own button-action broadcasts -->
                <action android:name="com.alfahrel.melody.WIDGET_SHUFFLE" />
                <action android:name="com.alfahrel.melody.WIDGET_PLAY" />
                <action android:name="com.alfahrel.melody.WIDGET_NEXT" />
                <action android:name="ACTION_PLAYBACK_STATE_CHANGED"/>

                <!-- Fired by MusicService when the playing track changes -->
                <action android:name="com.alfahrel.melody.ACTION_MUSIC_UPDATED" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/melody_widget_info" />
        </receiver>

        <activity
            android:name=".widget.WidgetConfigActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

    </application>

</manifest>